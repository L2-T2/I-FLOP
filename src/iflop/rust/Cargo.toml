[package]
name = "iflop"
version = "1.0.0"
edition = "2021"
description = "Dependency-free Rust core for FLOP and I-FLOP methods"

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1

[dependencies]
