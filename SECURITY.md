# Security

This package does not process untrusted code. It does parse numeric datasets and
native-backend request files, so callers should treat input data as untrusted
data and keep execution inside a normal user account.

Please report reproducible crashes, unsafe behavior, or malformed-input bugs in
the release repository issue tracker.
